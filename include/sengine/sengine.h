#pragma once

#define SENGINE_EXPORT __declspec(dllexport)

namespace SEngine
{
    class SEngine
    {  
        public:
            SEngine();
            ~SEngine();
    };

} // namespace SEngine
